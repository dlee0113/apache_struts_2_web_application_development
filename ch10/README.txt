This chapter's code is entirely JavaScript, although it's
in a normal Java webapp location.

JavaScript sources are named after the page they appear on,
with further information appended to the filename when it
seems necessary.

Some of the source lives in HTML files for easy loading.
